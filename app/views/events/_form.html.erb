<%= form_for(event) do |f| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :team %>
    <%= f.text_field :team %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :kind %>
    <%= f.text_field :kind %>
  </div>

  <div class="field">
    <%= f.label :other %>
    <%= f.text_area :other %>
  </div>

  <div class="field">
    <%= f.label :proposed_space %>
    <%= f.text_area :proposed_space %>
  </div>

  <div class="field">
    <%= f.label :proposed_time %>
    <%= f.text_area :proposed_time %>
  </div>

  <div class="field">
    <%= f.label :submitter_id %>
    <%= f.text_field :submitter_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
