Τελευταία ενημέρωση: <%= I18n.l DateTime.now, locale: :el %> (#<%= Audited::Audit.last.id %>)

<% if Settings.schedule.draft %>
**Πρόχειρο**
<% end %>

# <%= Settings.schedule.title_pw %>

<% c_id = 0 %>
<% @events.each_with_index do |event, idx| %>
  <% first = true %>
  <% if event.hidden %>
          <% next %>
      <% elsif (Settings.schedule.hide_unscheduled) and event.repetitions.empty? %>
          <% next %>
      <% end %>

<% if Settings.schedule.debug_indexes %>## Δράση <%= idx %> (#<%= event.id %>)<% end %>

<% dates_only = event.date_dates_dates.map{ |date| I18n.l date, format: :quick, locale: :el } %>

**Είδος**: <%= event.fields.map{|fiel| t fiel, locale: :el}.join(', ') %><%= '  ' %>
**Τίτλος**: <%= event.title %><%= '  ' %>
**Σύντομη περιγραφή**: <%= event.abstract.to_s.strip %><%= '  ' %>
**Διοργάνωση**: <%= event.sanitized_organiser %><%= '  ' %><% if event.active_submitters.any? %>
<% if event.active_submitters.many? %>
  **<%= event.active_submitters&.none?(&:male?) ? "Υπεύθυνες" : "Υπεύθυνοι" %>**:
<% else %>
  **<%= event.active_submitters&.first&.male? ? "Υπεύθυνος" : "Υπεύθυνη" %>**:
<% end %>

<% event.active_submitters.each do |submi| %>
  * <%= submi.full_name %>, <%= submi.property.gsub(/Αν[α|ά]πλ\./, 'Αν.') %>
<% end %><%= '  ' %><% end %>
**Τόπος**: <%= event.space %><%= '  ' %><% unless event.date_duration.to_s.blank? %>
**Διάρκεια**: <%= event.date_duration %>'<%= '  ' %><% end %>
**Ηλικίες που απευθύνεται**: <%= event.ages.to_s.gsub(/[[:digit:]]+/, '**\0**') %><%= '  ' %>
**<%= dates_only.many? ? "Ημερομηνίες" : "Ημερομηνία" %> διεξαγωγής**: <%= dates_only.join(', ') %><%= '  ' %>
**<%= (event.repetitions.group_by{|rep| rep.date.to_date}.first&.second&.size || 0) != 1 ? "Ώρες" : "Ώρα" %> έναρξης της δράσης**:

<% if (event.repetitions.group_by{|rep| rep.date.to_date}.first&.second&.size || 0) >= 7 %>
<% repetitions = event.repetitions.sort_by(&:date) %>
<%= I18n.l repetitions.first.date.to_time, format: :sched, locale: :el %> – <%= I18n.l (repetitions.last.date.to_time + (event.date_duration&.minutes || 0)), format: :sched, locale: :el %>
κάθε <%= ((event&.date_duration || 0) + get_interval(event)).round %>'
<% else %>
<% event.repetitions.group_by{|repet| I18n.l repet.date.to_time, format: :sched, locale: :el }.each do |time, repet| %>
  * <%= time %>
<% end %><% end %><%= '  ' %>
<% if event.registration_required %>**Δήλωση Συμμετοχής (απαιτείται)**: <%= event.registration_email %>
<%- end %>

---------------
<% end %>

